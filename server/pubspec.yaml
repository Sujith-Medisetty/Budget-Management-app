name: pocket_server
description: "Gmail Pub/Sub relay for Pocket — dumb forwarder, no parsing."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.13.2

dependencies:
  shelf: ^1.4.2
  shelf_router: ^1.1.4
  shelf_cors_headers: ^0.1.5
  dio: ^5.7.0
  googleapis: ^17.0.0
  googleapis_auth: ^2.3.3
  cryptography: ^2.7.0
  crypto: ^3.0.7
  dart_jsonwebtoken: ^2.12.2
  http: ^1.2.2
  dotenv: ^4.2.0
  logging: ^1.3.0
  meta: ^1.19.0
  postgres: ^3.5.12
  timezone: ^0.10.0

dev_dependencies:
  test: ^1.25.0
  lints: ^6.0.0
